<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MakersMarkt.Dashboards.SellerOrdersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MakersMarkt.Dashboards"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="White">

    <Grid Padding="40">
        <Border Background="#e8e8e8" CornerRadius="4" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="← Back"
                            Background="#444444"
                            Foreground="White"
                            FontWeight="SemiBold"
                            Click="BackButton_Click"
                            VerticalAlignment="Center"
                            Margin="0,0,16,0"/>

                    <StackPanel Grid.Column="1">
                        <TextBlock x:Name="WelcomeText"
                                   FontSize="14"
                                   Foreground="#666666"
                                   Margin="0,0,0,2"/>
                        <TextBlock Text="My Orders"
                                   FontSize="28"
                                   FontWeight="Bold"/>
                    </StackPanel>
                </Grid>

                <!-- ORDER LIST -->
                <Border Grid.Row="1"
                        Margin="0,24,0,0"
                        Background="White"
                        Padding="12"
                        CornerRadius="2">
                    <ListView x:Name="OrdersListView"
                              SelectionMode="None">
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:SellerOrderViewModel">
                                <Grid Margin="8">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="{x:Bind OrderTitle}"
                                                   FontWeight="SemiBold"
                                                   FontSize="15"/>
                                        <TextBlock Text="{x:Bind BuyerName}"
                                                   Foreground="#666666"
                                                   FontSize="13"/>
                                        <StackPanel Orientation="Horizontal" Spacing="12">
                                            <TextBlock Text="{x:Bind OrderDate}"
                                                       Foreground="#999999"
                                                       FontSize="12"/>
                                            <TextBlock Text="{x:Bind StatusDisplay}"
                                                       Foreground="{x:Bind StatusColor}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"/>
                                        </StackPanel>
                                        <TextBlock Text="{x:Bind ProductsSummary}"
                                                   Foreground="#999999"
                                                   FontSize="12"
                                                   TextTrimming="CharacterEllipsis"
                                                   MaxLines="1"/>
                                    </StackPanel>

                                    <Button Grid.Column="1"
                                            Content="Manage"
                                            Background="#0c5adb"
                                            Foreground="White"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Right"
                                            Click="Manage_Click"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Border>
            </Grid>
        </Border>
    </Grid>
</Page>